Telegram Bot for converting currency. 
For converting currency bot connecting to the official bank source and fetch currency exchange ratio during messaging. 
Also, programm contributes last 10 queries as history of requests into JSON 

@currency_explorer_bot tag for search in Telegram.

In order to play with him just:

  pip install -r requirements.txt

  Run code in your IDE.

